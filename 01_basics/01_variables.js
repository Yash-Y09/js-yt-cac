console.log("hihi");

const id = 1242 // this assign value can not change in future referance, Use for non changeable values
let name = "King" // changeable, block scope
var age = 100 //use for assign value but it visible in window property, changeable
city = "ahmedabad" // also treat as variable

